<%= form_for(@prestep) do |f| %>
  <% if @prestep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prestep.errors.count, "error") %> prohibited this prestep from being saved:</h2>

      <ul>
      <% @prestep.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sequence %><br />
    <%= f.number_field :sequence %>
  </div>
  <div class="field">
    <%= f.label :activity %><br />
    <%= f.text_field :activity %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.number_field :time %>
  </div>
    <%= f.hidden_field :recipe_id , :value => params[:recipe_id] %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
